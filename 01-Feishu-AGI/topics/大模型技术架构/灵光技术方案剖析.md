# 大模型技术架构

## 8. [2026-01-15]

## 📓 文章 6


> 文档 ID: `IjDNwBJRuiUKlZk4U2RcjyArnge`

**来源**: 灵光技术方案剖析 | **时间**: 2026-01-15 | **原文链接**: `https://mp.weixin.qq.com/s/IrR6ZhZQ...`

---

### 📋 核心分析

**战略价值**: 蚂蚁集团灵光 App 的完整技术架构拆解——从流式 HTML 全模态输出、组件化渲染、闪应用 iframe 沙箱，到万能自然语言数据查询接口，提供一套可复刻的 Agent 时代生成式 UI 工程范式。

**核心逻辑**:

- **整体流程**：用户提问 → 网络搜索收集信息 → 模型流式生成全模态 HTML 回复 → 按需触发 `generate_flash_app` 工具生成闪应用 → 前端以 iframe 加载渲染
- **全模态输出选 HTML 直出**：模型直接输出 HTML+CSS+JS，配合内置组件库保持样式统一，天然支持交互；代价是需要额外处理安全风险（XSS/注入），而非选择扩展 Markdown 方案
- **流式 HTML 数据结构**：每个流式片段携带 `templateId: "html"`、`templateType: "NATIVE"`，`templateData.text` 字段承载 HTML 片段，前端持续接收并触发增量渲染
- **组件化渲染机制**：HTML 中用 `component-type` 属性标识组件类型（`image`/`echarts`/`video`），JS 运行时内置 `ComponentRegistry` 注册组件状态，`componentMonitor` 上报 `init/succ/error` 三态
- **闪应用触发机制（推测）**：模型调用 `generate_flash_app` 工具，输入为自然语言需求描述，工具立即返回 `artifact_id`，后台另起 Agent 异步生成；主回复 HTML 中先渲染占位容器，前端轮询状态，完成后以 iframe 嵌入
- **闪应用双层架构**：内层是模型按需生成的应用代码，外层是通用基础层（`base.js` SDK + React 运行时 + Tailwind 样式），整体嵌入 iframe
- **万能查询接口 `lingguang.data.fetch(query, schema)`**：用自然语言描述数据需求 + JSON Schema 定义返回结构，后台 Agent 驱动搜索+爬取+LLM 提炼，直接返回结构化数据——彻底解耦"先备数据再生成应用"的串行依赖
- **跨端通信用 CandyJar 协议**：类 JsBridge 机制，闪应用（iframe）与宿主通信，支持千问 App 和 Web 端灵光统一调用，屏蔽平台差异
- **安全策略三层叠加**：CSP 白名单限制资源域 + `form-action: none` 禁止表单提交 + iframe `sandbox` 属性精细授权，模型输出视为不可信内容
- **样式方案**：Tailwind 提供样式变量，模型可直接引用变量或自行编写 CSS，复杂度自适应

---

### 🎯 关键洞察

**万能查询接口的范式价值**

传统方案：先准备数据结构 → 再让模型生成应用（串行，数据结构未必满足需求）

灵光方案：模型生成应用时直接声明数据需求 → 后台 Agent 实时获取并结构化返回（并行，按需索取）

这个接口本质上是把"数据层"变成了一个可被自然语言驱动的动态服务，应用生成和数据获取解耦，是 Agent 时代平台接口的核心范式转变。

**安全模型的设计逻辑**

模型输出 = 不可信代码。灵光的应对策略是：不阻止模型输出 JS（因为需要交互能力），而是通过 CSP + sandbox + form-action 三层围栏限制其能触达的资源范围，把攻击面收窄到白名单域名内。

---

### 📦 配置/工具详表

| 模块/功能 | 关键设置/代码 | 预期效果 | 注意事项/坑 |
|----------|-------------|---------|-----------|
| 流式 HTML 输出 | `templateId: "html"`, `templateType: "NATIVE"` | 增量渲染富文本 | 模型输出视为不可信，需 CSP 兜底 |
| 图片组件 | `<div component-type="image" id="image_8805">` + loading placeholder | 异步加载图片，失败降级报错 | 调用 `setContentErrorNode` + `componentMonitor error` 上报 |
| ECharts 组件 | `<div component-type="echarts" style="width:100%;height:400px;">` + `loadScript("https://xxx.alipayobjects.com/echarts.min.js")` | 动态加载库后渲染图表 | 需上报 `init/succ/error` 三态；捕获异常调 `setContentErrorNode` |
| 视频组件 | `VIDEO_WHITELIST = ['mp4', 'webm', 'ogg']` | 白名单格式才渲染 | 非白名单类型直接 `setContentErrorNode("illegal component type")` |
| 闪应用占位容器 | `artifact_id="flashapp-xxx"`, `data-async-render-node="params"`, `handleArtifactTimeout(120)` | 异步等待应用生成，120s 超时 | Lottie 动画加载失败时降级为 `component-loading` class |
| 存储 API | `lingguang.storage.setItem/getItem/removeItem/clear()` | 数据存服务端 | 服务不可用时降级为 `localStorage` |
| 数据查询 API | `lingguang.data.fetch(query, schema)` | 自然语言 → 结构化数据 | 后台 Agent 驱动，延迟不可控 |
| AI 图片生成 | `lingguang.ai.imageGeneration(options)` | 闪应用内生成图片 | — |
| 定位 | `lingguang.getLocation(options)` | 获取用户位置 | — |
| 语音 | `lingguang.playTTS(options)` / `lingguang.stopAllTTS()` | 语音播报/停止 | — |
| 地图 | `lingguang.loadMap(options)` | 加载高德地图 | CSP 已放行 `*.amap.com` |
| 埋点 | `lingguang.tracker.click(event, params)` / `.exposure(event, params)` | 点击/曝光上报 | — |
| CSP | `form-action: none; object-src: none; frame-src: self *.alipay.com` | 禁止表单提交、插件、跨域 frame | `unsafe-inline` + `unsafe-eval` 已开放，安全依赖域名白名单 |
| iframe sandbox | `allow-scripts allow-same-origin allow-popups allow-modals` | 允许 JS 和同源请求，禁止表单和顶层导航 | 无 `allow-forms`、无 `allow-top-navigation` |

---

### 🛠️ 操作流程

1. **准备阶段（复刻全模态输出）**
   - 搭建流式 SSE/WebSocket 接口，返回结构固定为 `{templateId, templateType, templateData: {text}}`
   - 引入组件库（ECharts、Lottie、视频播放器），注册 `ComponentRegistry`，实现 `componentMonitor(id, status, type, payload)` 上报函数
   - 配置 Tailwind CSS 作为样式基础层

2. **核心执行（闪应用生成链路）**
   - 主 Agent 回复时调用 `generate_flash_app` 工具，传入自然语言需求，立即拿到 `artifact_id`
   - 主回复 HTML 中插入占位 `<div artifact_id="flashapp-xxx" data-async-render-node="params">`，内嵌 `triggerMiniApp(JSON)` 脚本和 `handleArtifactTimeout(120)` 超时控制
   - 后台独立 Agent 异步生成闪应用代码（内层：模型生成；外层：base.js SDK + React + Tailwind 打包）
   - 前端轮询生成状态，完成后以 iframe 嵌入，sandbox 属性设为 `allow-scripts allow-same-origin allow-popups allow-modals`

3. **验证与优化（安全 + 数据）**
   - 在 iframe 宿主页写入完整 CSP meta 标签，白名单域名按实际业务配置（参考上方表格）
   - 实现 `lingguang.data.fetch`：接收 `query`（自然语言）+ `schema`（JSON Schema），后台走 GeneralSearch 工具检索 → Fetch 爬取渲染 JS → LLM 提炼 → 按 schema 结构化返回
   - 存储 API 优先对接服务端，降级逻辑写 `localStorage` fallback
   - 跨端通信实现 CandyJar 协议层（类 JsBridge），统一 Web 端和 App 端的 `lingguang.*` API 调用

---

### 💡 具体案例/数据

**ECharts 完整渲染代码**（可直接复用）：
```javascript
componentMonitor("echarts_0000", "init", "echarts", {
    backendTime: 00000000000000
});

loadScript("https://xxx.alipayobjects.com/echarts.min.js", () => {
    try {
        const chartDom = document.getElementById("echarts_0000");
        const myChart = echarts.init(chartDom, "myTheme");
        const option = {
            xAxis: { type: 'category', data: ['Mon', 'Tue', 'Wed'] },
            yAxis: { type: 'value' },
            series: [{
                type: 'candlestick',
                data: [[39.4, 40.28, 39.22, 40.5], [40.6, 39.34, 39.14, 40.6]]
            }]
        };
        myChart.setOption(option);
        componentMonitor("echarts_0000", "succ", "echarts");
    } catch (e) {
        setContentErrorNode("echarts_0000", "图表代码生成错误");
        componentMonitor("echarts_0000", "error", "echarts", { errorMsg: e.message });
    }
});
```

**万能查询接口调用示例**（A股热力图）：
```javascript
const data = await lingguang.data.fetch(
    "A股行业板块近5日板块轮动热力图数据，返回10个主要板块",
    {
        type: "object",
        properties: {
            heatmap: {
                type: "array",
                description: "热力图数据",
                items: {
                    type: "object",
                    properties: {
                        sector: { type: "string", description: "板块名称" },
                        day1: { type: "number", description: "第1天涨跌幅(%)" },
                        day2: { type: "number", description: "第2天涨跌幅(%)" },
                        day3: { type: "number", description: "第3天涨跌幅(%)" },
                        day4: { type: "number", description: "第4天涨跌幅(%)" },
                        day5: { type: "number", description: "第5天涨跌幅(%)" }
                    }
                }
            },
            updateTime: { type: "string", description: "数据更新时间" }
        }
    }
);
// 返回示例：
// { heatmap: [{ sector: "半导体", day1: 2.3, day2: -0.5, ... }], updateTime: "2026-01-14 15:00:00" }
```

**闪应用占位容器完整 HTML**：
```html
<div
  id="mini_app_dom"
  class="component-default"
  data-async-render-node="params"
  artifact_id="flashapp-xxxxxxxxxx"
  artifact_version="1"
  persistent-node="true"
>
  <div id="mini_app_dom_loading" class="lottie-loading">
    <div id="mini_app_dom_lottie_container" class="lottie-container">
      <script>
        loadScript(
          "https://gw.alipayobjects.com/os/lib/lottie-web/5.12.2/build/player/lottie.min.js",
          function () {
            lottie.loadAnimation({
              container: document.getElementById("mini_app_dom_lottie_container"),
              renderer: "svg", loop: true, autoplay: true,
              path: "https://mdn.alipayobjects.com/asap_serivce/uri/file/as/mini_app_pc_dark.1117.v1"
            });
          },
          function () {
            document.getElementById("mini_app_dom").classList.add("component-loading");
          }
        );
      </script>
      <script>
        loadScript(
          "https://render.alipay.com/p/yuyan/180020010001280215/progress-loading.202509281700.min.js",
          function () { progressLoading.show("mini_app_dom", 50000, false); },
          function () { console.error("progress-loading.js load failed"); }
        );
      </script>
    </div>
  </div>
  <script>
    triggerMiniApp('{"artifact_id":"flashapp-xxxxxxx","artifact_name":"小米汽车股票价格","artifact_type":"2","version_number":"1","is_finished":false,"core_value":"查询股票价格"}');
  </script>
  <script>handleArtifactTimeout(120);</script>
</div>
```

---

### 📝 避坑指南

- ⚠️ **`unsafe-inline` + `unsafe-eval` 已开放**：CSP 中这两项是为了支持模型生成的内联 JS，安全边界完全依赖域名白名单，白名单配置错误会直接导致安全漏洞
- ⚠️ **iframe sandbox 缺 `allow-same-origin` 会导致 base.js SDK 无法通信**：`allow-same-origin` 是 CandyJar 协议跨 iframe 通信的前提，不能删
- ⚠️ **`handleArtifactTimeout(120)` 是硬超时**：闪应用生成超过 120 秒直接失败，后台 Agent 生成复杂应用时需控制在此范围内
- ⚠️ **`lingguang.storage` 降级为 `localStorage` 时数据不跨设备**：服务端存储不可用时静默降级，开发时需测试降级路径
- ⚠️ **视频组件白名单只有 `['mp4', 'webm', 'ogg']`**：模型生成视频组件时若输出其他格式（如 `mov`、`avi`）直接报错，需在 Prompt 中约束格式
- ⚠️ **`form-action: none` 会拦截所有表单**：闪应用内如有登录/搜索表单需求，必须改用 JS fetch 替代原生 form submit

---

### 🏷️ 行业标签

#生成式UI #Agent架构 #闪应用 #全模态输出 #蚂蚁集团 #灵光 #JsBridge #CSP安全 #自然语言数据查询 #前端沙箱

---

---
