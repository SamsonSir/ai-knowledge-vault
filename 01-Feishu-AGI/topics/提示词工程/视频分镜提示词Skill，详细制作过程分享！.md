# 提示词工程

## 17. [2026-03-01]

## 📒 文章 7


> 文档 ID: `EZkdwlEajiBog3k1DS4cfl4GnMh`

**来源**: 视频分镜提示词Skill，详细制作过程分享！ | **时间**: 2026-02-28 | **原文链接**: `https://mp.weixin.qq.com/s/F5hVxpFw...`

---

### 📋 核心分析

**战略价值**: 从零构建一个「剧本→视频分镜提示词」Skill 的完整工业级 SOP，涵盖结构设计、脚本分工、提示词规范到封装测试的全链路复刻方法。

**核心逻辑**:

- **Skill 四层结构**：`SKILL.md`（定位与入口）+ `scripts/`（执行脚本）+ `references/`（规范知识库）+ `assets/`（交付模板），四层职责严格分离，互不干扰。
- **SKILL.md 是唯一必须存在的文件**，其余目录可按需增减，命名也可自定义。
- **主线思路三步走**：先明确"输入→流程→输出"，再拆分中间步骤，最后统一格式规范，避免做着做着跑偏。
- **七步完整工作流不可跳步**：剧本解析 → 角色提取 → 场景分析 → 分镜生成 → 提示词优化 → 一致性检查 → 导出。
- **统一结构规范是核心防线**：分镜表字段（镜头编号、景别、画面描述、构图、运镜、光线、色调、氛围、时长、镜头角度、提示词）必须在第四步就定死，所有后续脚本都读写同一模板，防止跨幕格式错乱。
- **脚本单独拆分的原因**：脚本负责执行，references 负责规则，assets 负责格式，三者职责不同；规则变了只改 references，逻辑变了只改 scripts，排错成本最低。
- **references 和 assets 建议多轮对话单独优化**，优化到满意后再放入目录，再让脚本调用指定路径，而不是一次性生成完就用。
- **修改时最省 token 的方法**：每次只让 AI 改一件事，明确说"只改哪个文件、不动其他文件、改完告诉我改了哪几行、怎么验证"，避免 AI 到处读文件大改代码。
- **SKILL.md 是否需要改的判断法**：用户使用方式变了 → 改 SKILL.md；只是内部实现变了 → 不用改。
- **提示词优化脚本的关键动作**：术语标准化 + 去重复提示词 + 补质量词（高质量、电影感、清晰对焦等万能词）+ 输出质量打分，让所有镜头风格统一，适配视频模型。
- **一致性检查脚本输出三件事**：问题 + 修复建议 + 复用规范提示词，不只报错，还给解法。

---

### 🎯 关键洞察

**为什么要先定结构再写脚本**：如果先写脚本再定格式，每个脚本的输出字段会各自为政，后期合并时字段对不上，修复成本极高。先在第四步把分镜表字段锁死，后面所有脚本都读写同一个模板，相当于给整个流程装了一条"轨道"，不会跑偏。

**references vs assets 的本质区别**：
- references = "规则说明书"，告诉脚本"怎么做才对"（术语、模板、规范、指南）
- assets = "空白表格"，提供可直接套用的格式文件（CSV 模板、HTML 模板、速查表）

两者都是为了"复用"，但 references 复用的是判断标准，assets 复用的是交付外观。

---

### 📦 配置/工具详表

| 模块 | 文件路径 | 职责 | 注意事项 |
|------|---------|------|---------|
| 入口定义 | `SKILL.md` | 定位、触发词、流程说明、输入输出规格 | 唯一必须存在的文件，触发词要覆盖用户可能说的各种表达 |
| 剧本解析 | `scripts/parse_script.py` | 读取 txt/md/docx/pdf/fdx，输出结构化 JSON | 优先保证稳健性，任何格式都能读入并形成场景列表 |
| 角色提取 | `scripts/character_extractor.py` | 提取性别/年龄/体型/发型/关键词，输出可直接用于 AI 生图的提示词 | 支持多场次服装变化追踪 |
| 场景分析 | `scripts/scene_analyzer.py` | 提取地点/室内外/时间段/光线/氛围，生成基础视觉提示词 | 这层是全片视觉基础，后续镜头在此叠加 |
| 分镜生成 | `scripts/storyboard_generator.py` | 按场景拆镜头，至少含建立镜头/角色出场/对话镜头 | 先保证"有镜头可用"，再优化艺术性 |
| 提示词优化 | `scripts/prompt_optimizer.py` | 术语标准化、去重、补质量词、质量打分 | 参考 references/ 中的提示词模板和镜头术语表 |
| 一致性检查 | `scripts/consistency_checker.py` | 检查角色跨镜漂移/场景跳变/光线突变 | 输出"问题+修复建议+复用规范提示词"三件套 |
| 导出 | `scripts/export_utils.py` | 输出 JSON/CSV/Markdown/Excel/HTML | HTML 基于 assets/ 中的模板生成 |
| 剧本格式规范 | `references/screenplay_format_spec.md` | 定义支持的剧本格式标准 | 单独文件方便后期只改规则不动脚本 |
| 角色设定模板 | `references/character_template.md` | 角色档案字段规范 | — |
| 场景设定模板 | `references/scene_template.md` | 场景字段规范 | — |
| 景别/运镜术语 | `references/shot_terminology.md` | 大特写/特写/中近景/中景/中远景/远景/大远景；固定/推/拉/摇/移/跟 | 提示词优化脚本必须调用此文件做术语标准化 |
| 情绪词库 | `references/mood_keywords_library.md` | 情绪氛围关键词集合 | — |
| 视频风格指南 | `references/video_style_guide.md` | 不同风格的视觉规范 | 可针对不同视频模型单独制作 |
| 一致性控制指南 | `references/consistency_control.md` | 跨镜头一致性规则 | — |
| 高效提示词模式 | `references/prompt_patterns.md` | 提示词写法模板库 | — |
| 分镜 CSV 模板 | `assets/storyboard_template.csv` | 统一分镜表格式 | 第四步定好字段后生成，所有脚本读写此模板 |
| HTML 导出模板 | `assets/export_template.html` | 可视化导出格式 | 导出脚本基于此模板生成 HTML |

---

### 🛠️ 操作流程

**前期：制定标准**

1. **明确总工作流** — 锁定七步顺序：剧本解析 → 角色提取 → 场景分析 → 分镜生成 → 提示词优化 → 一致性检查 → 导出。没有特别要求时永远执行完整七步，不跳步不合并。

2. **确定输入格式** — 支持 txt/md/docx/pdf/fdx。最低输入：一段话甚至一句话（但需提示用户风格一致性不可控）。推荐输入：剧本 + 角色参考图 + 风格参考图 + 画幅要求 + 时长限制。用户提供的参考图永远优先于 AI 推断。

3. **明确最终产出** — 五样东西：角色设定文档 + 场景设定文档 + 完整分镜提示词表 + 一致性检查报告 + 多格式导出文件（CSV/Markdown/Excel/HTML）。先写清楚产出，防止中途越做越偏。

4. **设计统一结构规范** — 分镜表字段必须在这步锁死：`镜头编号 | 景别 | 画面描述 | 构图 | 运镜 | 光线 | 色调 | 氛围 | 时长 | 镜头角度 | 提示词`。生成 `assets/storyboard_template.csv` 作为所有脚本的读写基准。

**中期：优化功能模块**

5. **准备 references 内容** — 单独生成并优化以下文档，每个独立成文件：`screenplay_format_spec.md` / `character_template.md` / `scene_template.md` / `shot_terminology.md` / `mood_keywords_library.md` / `video_style_guide.md` / `consistency_control.md` / `prompt_patterns.md`。

6. **准备 assets 内容** — 基于第4步结构规范生成：`storyboard_template.csv` / 角色提示词模板 / `export_template.html` / 速查表。

7. **实现剧本解析脚本** (`parse_script.py`) — 读取剧本文件，识别场景标题（INT/EXT 或"第X场/场景X"）、角色名、对白、动作、转场，输出 JSON 字段：`标题`、`场景列表`、`全角色`、`全地点`、`总时长秒数`、`元数据`（含场景数/角色数/地点数）。规则：上一行是角色名则下一行优先判定为对白，否则判定为动作；没有场景标题时自动创建默认场景；空行跳过。优先保证稳健性，任何文本都能读入并形成场景列表。

8. **实现角色提取脚本** (`character_extractor.py`) — 从第7步 JSON 提取角色档案：性别/年龄段/体型/发型/关键词/服装（支持多场次变化追踪）/气质/标志性道具。每个角色输出一段可直接用于 AI 生图的完整提示词描述。

9. **实现场景分析脚本** (`scene_analyzer.py`) — 从每个场景提取：地点/室内外/时间段/光线类型与方向/色彩基调/氛围/天气/季节，为每个场景生成一条基础视觉提示词。

10. **实现分镜生成脚本** (`storyboard_generator.py`) — 按场景自动拆镜头，至少包含建立镜头/角色出场/对话镜头，每个镜头严格按第4步字段输出，先保证"有镜头可用"再优化艺术性。

11. **实现提示词优化脚本** (`prompt_optimizer.py`) — 统一处理：术语标准化（调用 `shot_terminology.md`）+ 去重复提示词 + 补质量词（高质量、电影感、清晰对焦等）+ 输出质量打分。参考 `references/prompt_patterns.md`。

12. **实现一致性检查脚本** (`consistency_checker.py`) — 检查：角色跨镜头提示词漂移 / 场景连续性跳变 / 光影风格突变。输出格式：问题 + 修复建议 + 复用规范提示词。参考 `references/consistency_control.md`。

**后期：测试优化与封装**

13. **确认导出格式** — 默认支持 JSON + CSV + Markdown，HTML 基于 `assets/export_template.html` 生成，支持按场次/角色/场景分类导出。

14. **上传剧本测试** — 用 2-3 份不同类型剧本跑全流程（可 AI 生成）。重点检查：字段是否缺失 / 镜头是否正确生成 / 提示词是否符合 references 规范。发现问题只修改对应脚本，不动其他文件。

15. **封装 SKILL.md** — 生成完整 SKILL.md，包含：定位说明 / 触发词 / 完整七步流程 / 输入要求 / 输出目标 / 文件目录结构。最终目录：`SKILL.md` + `scripts/` + `references/` + `assets/`。

---

### 💡 具体案例/数据

**SKILL.md YAML 头部示例**（触发词覆盖用户各种表达方式）:
```yaml
name: script-to-video-prompts
description: 短剧剧本转视频提示词生成器。将用户上传的短剧剧本文档智能拆解为可直接用于AI视频生成的完整中文提示词体系。输出包括：角色设定提示词、场景设定提示词、逐镜头分镜提示词。支持批量处理、多格式导出、一致性校验。当用户说"剧本转视频提示词"、"拆解剧本生成分镜"、"短剧转视频"、"批量生成分镜提示词"、"剧本可视化"时触发。
```

**剧本解析脚本提示词（详细版）**:
```
帮我生成脚本，用于把原始剧本内容（支持 txt/md/docx/pdf/fdx）解析成结构化 JSON，
要求自动识别并分类：场景标题（INT/EXT 或"第X场/场景X"）、角色名、对白、动作、转场，
并输出中文字段：`标题`、`场景列表`、`全角色`、`全地点`、`总时长秒数`、`元数据`
（含`场景数`、`角色数`、`地点数`），其中每个场景至少包含`场景编号`、`场景标题`、
`地点`、`时间段`、`内外景`、`角色`；
规则是"上一行是角色名则下一行优先判定为对白，否则判定为动作"，
没有场景标题时自动创建默认场景，空行跳过；
重点先保证稳健性和通用性，不追求复杂算法，
优先做到"任何剧本或任意文本内容都能读入并形成场景列表"；
支持通过命令行传入文件路径并输出格式化 JSON，代码尽量清晰、注释简单、依赖缺失时提示安装。
```

**剧本解析脚本提示词（简化版）**:
```
生成一个通用剧本解析脚本，能读取常见文本格式并智能分析内容，
自动识别场景、角色、对白、动作和转场，输出清晰的结构化 JSON；
要求优先保证稳定可用，即使输入不规范也能自动补默认场景并正常返回结果。
```

**完整 Skill 生成主提示词**（15步完整版，直接可用）:
```
你是一个 Skill 开发助手，帮我从零构建一个「剧本转视频分镜提示词」Skill。
请按以下规格和顺序执行，不可跳步，不可合并步骤。

【第一步：工作流程定义】
工作流程：剧本解析 → 角色提取 → 场景分析 → 分镜生成 → 提示词优化 → 一致性检查 → 导出。
不可跳步，不可合并步骤。用户没有特别要求时永远执行完整七步。

【第二步：输入规格定义】
支持 txt/md/docx/pdf/fdx。最低输入为一段话甚至一句话，但必须告知用户一致性风险。
推荐输入：剧本 + 角色参考图 + 风格参考图 + 目标画幅 + 目标时长 + 视觉风格关键词。
用户提供的参考图永远优先于推断。

【第三步：最终产出定义】
角色设定文档 + 场景设定文档 + 完整分镜提示词表 + 一致性检查报告 + 多格式导出文件（CSV/Markdown/Excel/HTML）。

【第四步：统一结构规范】
分镜表字段：镜头编号、景别、画面描述、构图、运镜、光线、色调、氛围、时长、镜头角度、提示词。
先生成统一表格模板，所有脚本读写此模板。

【第五步：references 内容】
生成独立文件：剧本格式规范、镜头术语表、情绪词库、提示词模板、一致性检查指南。

【第六步：assets 内容】
生成：分镜 CSV 模板、角色提示词模板、HTML 导出模板、速查表。

【第七步：剧本解析脚本】
读剧本文件，分出场景/角色/对白/动作，输出 JSON。代码清晰精炼、注释清楚。

【第八步：角色提取脚本】
提取角色档案（性别/年龄段/体型/发型/关键词），为每个角色输出可直接用于 AI 生图的提示词。

【第九步：场景分析脚本】
提取地点/室内外/时间段/光线/氛围，为每个场景生成基础视觉提示词。

【第十步：分镜生成脚本】
按场景拆镜头，至少含建立镜头/角色出场/对话镜头，严格按第四步字段输出。

【第十一步：提示词优化脚本】
术语标准化 + 去重 + 补质量词 + 质量打分。参考 references/ 中的提示词模板和镜头术语表。

【第十二步：一致性检查脚本】
检查角色跨镜漂移/场景跳变/光线突变，输出"问题+修复建议+复用规范提示词"。

【第十三步：导出脚本】
导出 JSON/CSV/Markdown 三种格式，HTML 基于 assets/ 中的模板生成。

【第十四步：测试】
用 2-3 份不同类型剧本跑全流程，检查字段缺失/镜头生成/提示词规范。
发现问题只修改对应脚本，不动其他文件。告诉我每份剧本的测试结果和问题。

【第十五步：封装 SKILL.md】
生成完整 SKILL.md：定位说明/触发方式/完整七步流程/输入要求/输出目标/文件目录结构。

所有文件生成完毕后，输出完整目录结构，以及如何用一份测试剧本跑通全流程的指南。
```

**最终输出结构**:
```
一、项目元数据（片名、集数、总时长、场次数）
二、风格总设定（画面风格、色彩体系、光影风格）
三、角色设定库（JSON 结构化数据 + 自然语言描述）
四、场景设定库（JSON 结构化数据 + 自然语言描述）
五、完整分镜提示词（按场次顺序，全部中文）
六、一致性参考表（角色/场景一致性种子词）
```

**Skill 资源下载**:
- Coze 直接使用：`https://www.coze.cn/?skill_share_pid=7596234767713173538`
- Skill 文件下载：`https://my.feishu.cn/docx/PsPfdVFD9oZ3nZxcvBFcGxdgnje`

---

### 📝 避坑指南

- ⚠️ **不要先写脚本再定格式**：字段没锁死就开始写脚本，各脚本输出字段会各自为政，后期合并时对不上，修复成本极高。
- ⚠️ **references 和 assets 不要一次性生成完就用**：建议多轮对话单独优化到满意，再放入目录让脚本调用，否则规范质量低会污染所有下游脚本。
- ⚠️ **修改时不要让 AI 一次改多个文件**：每次只指定一个文件路径，明确说"只改这里、不动其他文件、改完告诉我改了哪几行"，否则 AI 会到处读文件大改代码，token 消耗高且结果不稳定。
- ⚠️ **SKILL.md 不是每次都要改**：只有用户使用方式变了才改 SKILL.md；内部实现变了只改对应脚本。
- ⚠️ **输入不规范时要提前告知用户**：只输入一句话或一段话时，必须告知用户"风格一致性和走向可能不完全可控，建议补充更多信息"，不能静默处理。
- ⚠️ **提示词优化不能只靠脚本**：如果对视频提示词有特定要求，需要单独制作提示词参考文档（放 references）或格式模板（放 assets），并在脚本中显式调用指定路径，不能依赖 AI 自行推断。

---

### 🏷️ 行业标签

#Skill制作 #视频分镜 #提示词工程 #剧本自动化 #AI视频生成 #Coze #工作流设计 #Python脚本

---

---
